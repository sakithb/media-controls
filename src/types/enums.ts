export enum PanelElements {
    ICON = "Player icon",
    LABEL = "Label",
    CONTROLS = "Player controls",
}

export enum LabelTypes {
    ARTIST = "Artist",
    TITLE = "Title",
    ALBUM = "Album",
    DURATION = "Duration",
}
